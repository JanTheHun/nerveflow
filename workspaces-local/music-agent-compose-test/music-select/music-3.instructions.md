User query:
- ${event.value}

Candidates:
- Provided as a list, ordered by similarity (highest first).
- Each candidate has a unique "id" field.

Task:
Rank candidates by how relevant they are to user query.